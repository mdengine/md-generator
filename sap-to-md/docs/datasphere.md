# Datasphere

See pipeline v2 outputs under `datasphere/` when parsing `.dsview`, `.dataflow`, or analytical model JSON exports.

Cross-system `SAME_AS` edges include `SemanticMatch` confidence metadata.
