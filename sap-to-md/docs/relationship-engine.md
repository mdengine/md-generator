# Relationship engine

`networkx.MultiDiGraph` with typed edges:

- `READS_TABLE`, `FK`, `ASSOCIATION`, `COMPOSITION`, `CALLS`, `INCLUDES`

Sources: ABAP SQL, DDIC check tables, CDS associations, BAPI table parameters.

Exports: `graph-full.json`, Mermaid ER under `graphs/`.
