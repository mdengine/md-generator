# Skill Builder Compatibility

| Item | Support |
|------|---------|
| Python | 3.10+ (per `pyproject.toml`) |
| OS | Windows, Linux, macOS (module-specific native deps may vary) |
| PyPI extra | `(base package)` |
| Input | Project metadata |

Test your target input versions in CI with fixtures under `*-to-md/tests`.
