# OData Metadata Database Notes

This module does **not** use an application database. All state is ephemeral (temp directories, in-memory job tables) except where job SQLite stores are used by API services.
