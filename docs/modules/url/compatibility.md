# URL and Web Compatibility

| Item | Support |
|------|---------|
| Python | 3.10+ (per `pyproject.toml`) |
| OS | Windows, Linux, macOS (module-specific native deps may vary) |
| PyPI extra | `url or url-full` |
| Input | HTTP(S) pages |

Test your target input versions in CI with fixtures under `url-to-md/tests`.
